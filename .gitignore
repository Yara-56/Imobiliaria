# =============================================================================
# ENVIRONMENT / SECRETS (Prioridade Máxima)
# =============================================================================
.env
.env.local
.env.development
.env.production
.env.staging
.env.*.local

# Nunca ignorar o template de exemplo para que novos devs saibam o que configurar
!.env.example

# Certificados, chaves privadas e credenciais de nuvem
*.pem
*.key
*.p12
*.pfx
*.crt
.aws/
.gcloud/
service-account.json

# =============================================================================
# DEPENDENCIES
# =============================================================================
node_modules/
.pnp
.pnp.js

# Lockfiles de npm/yarn acidentais — o projeto usa pnpm (pnpm-lock.yaml deve ser commitado).
# Monorepo: lock só na raiz (não correr `pnpm install` dentro de apps/*).
apps/**/pnpm-lock.yaml
package-lock.json
npm-shrinkwrap.json
yarn.lock

# =============================================================================
# BUILD / OUTPUT
# =============================================================================
dist/
dist-ssr/
build/
bin/
out/
.next/
.vercel/
# Ignorar metadados do TS
*.tsbuildinfo

# =============================================================================
# LOGS & DEBUG
# =============================================================================
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Process IDs e dump de memória
*.pid
*.seed
*.pid.lock
core.[0-9]*

# =============================================================================
# TESTING & COVERAGE
# =============================================================================
coverage/
*.lcov
.nyc_output
__tests__/__snapshots__/
playwright-report/
test-results/
test-report/

# =============================================================================
# CACHE & TOOLS
# =============================================================================
.cache/
.eslintcache
.stylelintcache
.prettiercache
.vite/
.turbo/

# =============================================================================
# OS & IDE (System Specific)
# =============================================================================
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
desktop.ini

# IDE - VS Code
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json
# Nota: Manter launch.json e settings.json (sem segredos) ajuda o time a ter o mesmo setup

# IDE - JetBrains (WebStorm, IntelliJ)
.idea/
*.iws
*.iml
*.ipr

# =============================================================================
# BACKEND SPECIFIC (Uploads & DB)
# =============================================================================
# Importante: Ignorar a pasta de uploads local para não subir fotos de teste pro Git
uploads/
temp/
tmp/

# Prisma / Bancos Locais
/generated/
*.db
*.db-journal
query-engine-*
migration_lock.toml

# =============================================================================
# DOCKER
# =============================================================================
.docker/